# ════════════════════════════════
# JobPilot AI — Backend Dependencies
# ════════════════════════════════

# ── Web Framework ───────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.18

# ── Database ────────────────────────
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0

# ── Cache ───────────────────────────
redis[hiredis]>=5.0.0

# ── Task Queue ──────────────────────
celery>=5.4.0

# ── Auth (custom session-based, no external lib needed) ──

# ── AI / LLM ────────────────────────
openai>=1.40.0
anthropic>=0.27.0
google-generativeai>=0.8.0
requests>=2.32.0

# ── Validation & Serialization ──────
pydantic>=2.6.0
pydantic-settings>=2.2.0
email-validator>=2.2.0

# ── PDF Parsing ─────────────────────
pypdf>=4.0.0

# ── Observability ───────────────────
structlog>=24.1.0
sentry-sdk[fastapi]>=2.0.0
prometheus-client>=0.20.0

# ── Security ────────────────────────
cryptography>=42.0.0

# ── Utility ────────────────────────
python-dotenv>=1.0.0
httpx>=0.27.0
Jinja2>=3.1.0

# ── Vector Search ──────────────────
pgvector>=0.3.0
